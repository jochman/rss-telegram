[project]
name = "rss-telegram"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.12"
dependencies = [
    "feedparser>=6.0.12",
    "python-telegram-bot==20.7",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
    "ty>=0.0.25",
]
